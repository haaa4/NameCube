<Page
    x:Class="NameCube.ToolBox.AutomaticProcessPages.ProcessSettingPages.AudioSettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NameCube.ToolBox.AutomaticProcessPages.ProcessSettingPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="AudioSettingPag"
    d:DesignHeight="350"
    d:DesignWidth="410"
    Background="#f5f1f1"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="60" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="音频文件" />
        <ComboBox
            x:Name="AudioComboBox"
            Grid.Column="1"
            Margin="5"
            SelectionChanged="AudioComboBox_SelectionChanged" />
        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="播放时间" />
        <ui:NumberBox
            x:Name="WaitTimeNumberBox"
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            LostFocus="WaitTimeNumberBox_LostFocus"
            Maximum="3600"
            Minimum="0"
            ValueChanged="WaitTimeNumberBox_ValueChanged" />
        <TextBlock
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Margin="5"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Foreground="Gray"
            Text="注：当时间为0时，则播放至音频结束" />
    </Grid>
</Page>
