<Page
    x:Class="NameCube.ToolBox.AutomaticProcessPages.ProcessPages.ReadyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NameCube.ToolBox.AutomaticProcessPages.ProcessPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="ReadyPage"
    d:DesignHeight="350"
    d:DesignWidth="600"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock
            Margin="10,10"
            FontSize="30"
            Text="{Binding Info}"
            TextWrapping="Wrap" />
        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="40"
            Text="{Binding ProcessName}"
            TextWrapping="Wrap" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ui:Button
                Width="120"
                HorizontalAlignment="Center"
                Appearance="Primary"
                Click="Button_Click"
                Content="立刻执行" />
            <ui:Button
                x:Name="CancleButton"
                Grid.Column="1"
                Width="120"
                HorizontalAlignment="Center"
                Click="CancleButton_Click"
                Content="取消" />
        </Grid>
    </Grid>
</Page>
