<Page
    x:Class="NameCube.ToolBox.AutomaticProcess"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="clr-namespace:NameCube.ToolBox.AutomaticProcessPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NameCube.ToolBox"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="AutomaticProcess"
    d:DesignHeight="700"
    d:DesignWidth="880"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ui:InfoBar
            Title="警告"
            IsClosable="False"
            IsOpen="True"
            Message="自动流程处于实验阶段，请谨慎使用"
            Severity="Warning" />
        <ui:NavigationView
            x:Name="NavigationMenu"
            Grid.Row="1"
            MinHeight="300"
            Margin="0"
            IsBackButtonVisible="Auto"
            IsPaneToggleVisible="True"
            PaneDisplayMode="LeftFluent">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    Content="音频"
                    Icon="{ui:SymbolIcon Symbol=SoundWaveCircle24}"
                    TargetPageType="{x:Type a:Audio}" />
                <ui:NavigationViewItem
                    Content="流程组"
                    Icon="{ui:SymbolIcon Symbol=AlignSpaceEvenlyHorizontal24}"
                    TargetPageType="{x:Type a:AutomaticProcessSetting}" />
                <ui:NavigationViewItem
                    Content="时间表"
                    Icon="{ui:SymbolIcon Symbol=Timer24}"
                    TargetPageType="{x:Type a:SchedulePage}" />
                <ui:NavigationViewItem
                    x:Name="DebugItem"
                    Content="Debug"
                    Icon="{ui:SymbolIcon Symbol=Bug24}"
                    TargetPageType="{x:Type a:Debug}"
                    Visibility="Collapsed" />
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>
    </Grid>
</Page>
